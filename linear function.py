#linear function

print "set variable 'a' with at least one decimal place, e.g. 2.0"
def method_name():
    a = input()
    return a
a = method_name()
print "set variable 'b' with at least one decimal place, e.g. 2.0"
def method_name2():
    b = input()
    return b
b = method_name2()
if a == 0 and b == 0: print "infinitely many solutions"
if a == 0 and b != 0: print "no solutions"
if a > 0.0: print -b / a

    # <> with <3 by JR
